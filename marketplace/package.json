{
  "name": "mcpod-marketplace",
  "version": "0.1.0",
  "description": "Registry server for MCPod: serves a paged JSON index of MCP servers and their config.mcpod files.",
  "type": "module",
  "scripts": {
    "start": "node bin/server.js",
    "test": "node --test",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
